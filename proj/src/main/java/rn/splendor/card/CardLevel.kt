package rn.splendor.card

enum class CardLevel {
    L0,
    L1,
    L2;

//    companion object {
//        private val map = CardLevel.values().associateBy(CardLevel::index)
//        fun fromInt(index: Int) = map[index] ?: throw InvalidDataException("No gems with index $index")
//    }
}